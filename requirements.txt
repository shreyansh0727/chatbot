# Core Flask
flask==3.0.0
flask-cors==4.0.0
werkzeug==3.0.0

# WSGI Server
gunicorn==21.2.0

# Database
pymongo==4.6.0
dnspython==2.4.2

# Environment variables
python-dotenv==1.0.0

# Lightweight sentence transformers (CPU-only)
sentence-transformers==2.7.0

# PyTorch CPU-only (much smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchaudio==2.1.0+cpu
torchvision==0.16.0+cpu

# Core ML libraries (minimal versions)
scikit-learn==1.3.2
numpy==1.24.3

# Text processing
beautifulsoup4==4.12.3
requests==2.31.0
regex==2023.12.25

# Utilities
tqdm==4.66.1
filelock==3.13.1
huggingface-hub==0.20.0
safetensors==0.4.1
tokenizers==0.15.0
transformers==4.36.0

# Optional: spaCy (skip if still OOM)
# spacy==3.7.2
